stream {

    # [SSH] SIP
    server {
        listen 22;
        proxy_pass 10.100.10.22:22;
        proxy_timeout 3600s;
        proxy_connect_timeout 10s;
    }


    # [SIP] FreePBX SIP  (pbx.nettech.uz)
    server {
        listen 5060 udp;
        proxy_pass 10.100.10.220:5060;
        proxy_timeout 3600s;
        proxy_connect_timeout 10s;
    }

}