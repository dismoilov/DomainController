# Pin к версиям, которые прошли integration-проверку на продакшене.
# Python 3.11+ (на Ubuntu 24.04 — python3.12 по умолчанию).
# Обновляем осознанно: после изменения запустить `flask doctor` и проверить /healthz.

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.48
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
blinker==1.9.0
click==8.3.1
gunicorn==23.0.0
psycopg2-binary==2.9.10
# GeoIP lookup (offline через DB-IP free .mmdb).
# База — НЕ в pip-пакете, тянем отдельно через deploy/update-geoip.sh.
maxminddb==3.1.1
